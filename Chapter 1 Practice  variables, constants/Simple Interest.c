#include <stdio.h>

int main() {
	int rate= 10, year=4, amount=1000;
	int SimpleInterest=amount*rate*year/100;
	printf("The value of simple SimpleInterest is equal to %d",SimpleInterest);
	return 0;
}